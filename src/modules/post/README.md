# post module (placeholder)

The **post** module will hold shop-listing logic (create/search/filter shops —
FR-02 and FR-03 in the PRD: photos, size, location, price, lease terms).

This is intentionally left empty in Phase 1. This build only covers
**Auth & Users**, per the checklist. The folder exists now so the project
structure already matches the target layout.

When listings work starts, mirror the auth/user module shape:

- `post.dto.ts`
- `post.validation.ts`
- `post.service.ts`
- `post.controller.ts`
- `post.routes.ts`
- `index.ts`
